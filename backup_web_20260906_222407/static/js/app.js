let state={results:[],summary:{},sources:{},running:false};let activeFilter='VŠE';
const $=id=>document.getElementById(id);
const rowClass=r=>({"OK":"row-ok","CHYBÍ V UNIQA":"row-missing","NEPOJIŠTĚNO, ALE DEPOZIT":"row-deposit","PRODANÉ, ALE V UNIQA":"row-sold","NAVÍC V UNIQA":"row-extra","SPZ NESOUHLASÍ":"row-warning","NELZE OVĚŘIT":"row-error"}[r.status_raw]||'');
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function source(name,prefix){const s=state.sources[name]||{};$(prefix+'Dot').className='dot '+(s.state||'idle');$(prefix+'Status').textContent=s.status||'';$(prefix+'Detail').textContent=s.detail||'';}
function matches(r){if(activeFilter==='VŠE')return true;if(activeFilter==='ACTIVE')return ['OK','CHYBÍ V UNIQA','NEPOJIŠTĚNO, ALE DEPOZIT','SPZ NESOUHLASÍ','NELZE OVĚŘIT'].includes(r.status_raw);if(activeFilter==='OK_TOTAL')return r.status_raw==='OK';if(activeFilter==='OK_UNIQA')return r.status_raw==='OK'&&r.pojistovna==='UNIQA';if(activeFilter==='OK_ALLIANZ')return r.status_raw==='OK'&&r.pojistovna==='ALLIANZ';if(activeFilter==='MISSING')return r.status_raw==='CHYBÍ V UNIQA';if(activeFilter==='DEPOSIT')return r.status_raw==='NEPOJIŠTĚNO, ALE DEPOZIT';if(activeFilter==='SOLD_UNIQA')return r.status_raw==='PRODANÉ, ALE V UNIQA';if(activeFilter==='EXTRA_UNIQA')return r.status_raw==='NAVÍC V UNIQA';return true;}
function renderRows(){const q=$('search').value.trim().toUpperCase();const rows=state.results.filter(r=>matches(r)&&(!q||Object.values(r).join(' ').toUpperCase().includes(q)));$('footerRight').textContent='Zobrazeno: '+rows.length;if(!rows.length){$('rows').innerHTML='<tr class="empty-row"><td colspan="8" class="empty">Žádné výsledky pro zvolený filtr.</td></tr>';return;}$('rows').innerHTML=rows.map((r,i)=>`<tr class="${rowClass(r)}" data-index="${state.results.indexOf(r)}"><td>${esc(r.status)}</td><td>${esc(r.pojistovna)}</td><td>${esc(r.vin)}</td><td>${esc(r.spz_tir)}</td><td>${esc(r.spz_uniqa)}</td><td>${esc(r.vykup)}</td><td>${esc(r.prodej)}</td><td>${esc(r.detail)}</td></tr>`).join('');document.querySelectorAll('#rows tr[data-index]').forEach(tr=>tr.addEventListener('dblclick',()=>showDetail(state.results[Number(tr.dataset.index)])));}
function render(){const s=state.summary||{};$('cActive').textContent=s.active||0;$('cOkTotal').textContent=s.ok_total||0;$('cOkUniqa').textContent=s.ok_uniqa||0;$('cOkAllianz').textContent=s.ok_allianz||0;$('cMissing').textContent=s.missing||0;$('cDeposit').textContent=s.deposit||0;$('cSold').textContent=s.sold_uniqa||0;$('cExtra').textContent=s.extra_uniqa||0;source('tirbazar','tir');source('uniqa','uniqa');source('allianz','allianz');$('runBtn').disabled=state.running;$('runBtn').textContent=state.running?'PROBÍHÁ KONTROLA…':'▶ SPUSTIT KONTROLU';$('csvBtn').classList.toggle('disabled',!state.csv_available);const live=$('liveDot');if(state.running){live.className='dot loading';$('liveStatus').textContent='KONTROLA PROBÍHÁ';}else if(state.error){live.className='dot error';$('liveStatus').textContent='CHYBA';}else if(state.finished_at){live.className='dot ok';$('liveStatus').textContent='KONTROLA KOMPLETNÍ ✓';}else{live.className='dot idle';$('liveStatus').textContent='PŘIPRAVENO';}$('footerLeft').textContent=state.finished_at?'Dokončeno: '+state.finished_at:(state.started_at?'Spuštěno: '+state.started_at:'Připraveno');renderRows();}
async function refresh(){try{const r=await fetch('/api/state',{cache:'no-store'});state=await r.json();render();if(state.error)toast(state.error,true);}catch(e){toast('Nepodařilo se načíst stav aplikace.',true);}}
async function run(){try{const r=await fetch('/api/run',{method:'POST'});const d=await r.json();if(!r.ok)toast(d.message||'Kontrolu se nepodařilo spustit.',true);await refresh();}catch(e){toast('Kontrolu se nepodařilo spustit.',true);}}
function showDetail(r){$('detailBody').innerHTML=`<dl class="detail-grid"><dt>STATUS</dt><dd>${esc(r.status)}</dd><dt>POJIŠŤOVNA</dt><dd>${esc(r.pojistovna||'-')}</dd><dt>VIN</dt><dd>${esc(r.vin||'-')}</dd><dt>SPZ TIRBazar</dt><dd>${esc(r.spz_tir||'-')}</dd><dt>SPZ UNIQA</dt><dd>${esc(r.spz_uniqa||'-')}</dd><dt>Datum výkupu</dt><dd>${esc(r.vykup||'-')}</dd><dt>Datum prodeje</dt><dd>${esc(r.prodej||'-')}</dd><dt>Výsledek</dt><dd>${esc(r.detail||'-')}</dd></dl>`;$('detailDialog').showModal();}
let lastToast='';function toast(msg,error=false){if(!msg||msg===lastToast)return;lastToast=msg;const t=$('toast');t.textContent=msg;t.className='toast'+(error?' error':'');t.hidden=false;setTimeout(()=>{t.hidden=true;lastToast='';},5000);}
$('runBtn').addEventListener('click',run);$('search').addEventListener('input',renderRows);$('clearBtn').addEventListener('click',()=>{activeFilter='VŠE';$('search').value='';document.querySelectorAll('.card').forEach(c=>c.classList.remove('selected'));renderRows();});document.querySelectorAll('.card').forEach(c=>c.addEventListener('click',()=>{activeFilter=c.dataset.filter;document.querySelectorAll('.card').forEach(x=>x.classList.toggle('selected',x===c));renderRows();}));$('closeDialog').addEventListener('click',()=>$('detailDialog').close());setInterval(refresh,2000);refresh();
